package com.taskmanager.domain.enums;

/**
 * Niveles de prioridad para tareas.
 * Mapea directamente al ENUM prioridad_enum de PostgreSQL.
 */
public enum Prioridad {
    BAJA,
    MEDIA,
    ALTA,
    URGENTE
}
